import { apiData,apiError } from "@/lib/github/http"; import { getActivity,getContributors,getLanguages,getRepo } from "@/lib/github/client"; import { repoPartSchema } from "@/lib/validation";
export async function GET(_:Request,{params}:{params:Promise<{owner:string;repo:string}>}){try{const p=await params,owner=repoPartSchema.parse(p.owner),repo=repoPartSchema.parse(p.repo);const [repository,languages,contributors,activity]=await Promise.all([getRepo(owner,repo),getLanguages(owner,repo),getContributors(owner,repo),getActivity(owner,repo)]);return apiData({repository,languages,contributors,activity});}catch(e){return apiError(e)}}
