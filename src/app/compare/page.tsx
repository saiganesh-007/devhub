import type { Metadata } from "next"; import { AppShell,RateBadge } from "@/components/shell"; import { CompareExperience } from "@/components/compare-experience";
export const metadata:Metadata={title:"Compare"}; export default function ComparePage(){return <AppShell><RateBadge/><h1 className="mt-5 text-4xl font-semibold tracking-[-.04em] text-white">Compare the signals.<br/><span className="text-zinc-500">Keep the judgment human.</span></h1><p className="mt-4 max-w-2xl text-zinc-500">Side-by-side factual GitHub metrics. DevHub never declares a winner or invents a quality score.</p><div className="mt-10"><CompareExperience/></div></AppShell>}
